---
description: 项目概述与技术栈锁定
globs: 
alwaysApply: true
---

# 企业级 AI Agent 应用商店 — 项目总则

## 项目背景
企业级 AI Agent 应用商店，支持内部技能和外部技能的分发、安全扫描、审批和生态运营。

## 技术栈（锁定，不得自行变更）

| 层 | 技术 |
|----|------|
| 前端 | Vue 3 + Vite + Element Plus + Pinia + TypeScript |
| 后端 | FastAPI + SQLAlchemy (async) + Pydantic |
| 数据库 | PostgreSQL 16 |
| 对象存储 | MinIO (S3 兼容) |
| 搜索 | OpenSearch 2.x |
| 认证 | JWT (python-jose + passlib) |
| 容器化 | Docker + Docker Compose |
| 迁移 | Alembic |

**硬约束：前端不用 Next.js/React，后端不用 Spring/Java。**

## 必读文档

执行任务前，先读取以下 `.cursor/rules/` 文件：
- @backend-rules — 后端编码规范
- @frontend-rules — 前端编码规范
- @ui-design — UI 设计规范
- @api-conventions — API 命名规范
- @testing — 测试要求

## 禁止事项
- ❌ 不修改技术栈
- ❌ 不硬编码密钥（放 .env）
- ❌ 不使用 SELECT *
- ❌ 不暴露内部错误堆栈
- ❌ 不删除他人代码（除非任务明确要求）
