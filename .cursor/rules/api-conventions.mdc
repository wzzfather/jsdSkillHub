---
description: API 命名与接口规范
globs: backend/**
alwaysApply: false
---

# API 接口规范

## RESTful 命名
```
GET    /api/apps              # 列表（分页/筛选/排序）
GET    /api/apps/{id}         # 详情
POST   /api/apps              # 创建
PUT    /api/apps/{id}         # 更新
DELETE /api/apps/{id}         # 删除
POST   /api/apps/{id}/publish # 状态变更
```

## 分页参数
- page: 从 1 开始
- page_size: 默认 20，最大 100

## 响应格式
```json
{
  "items": [...],
  "total": 100,
  "page": 1,
  "page_size": 20
}
```

## 错误响应
```json
{
  "detail": "错误描述",
  "code": "ERROR_CODE"
}
```

## HTTP 状态码
- 200 成功
- 201 创建成功
- 400 参数错误
- 401 未认证
- 403 权限不足
- 404 资源不存在
- 422 参数校验失败
