## 任务：D-015 邮箱认证 + D-016 上架下架流程
- **目标**：User 邮箱字段与验证码接口；Skill offline/resubmit/republish 与前端联动
- **状态**：已完成（需在本地执行 `npm run build` 与后端启动自检）
- **产出**：`backend/app/models/user.py`, `skill.py`, `seed.py`, `schemas/common.py`, `routers/auth.py`, `skills.py`, `requirements.txt`；`frontend` Register/Verify/MyApps、api、router
- **说明**：终端工具未执行成功，请本地运行 `npm run build`

---

## 任务：ExploreView 服务端筛选 + 管理员应用管理页
- **目标**：`GET /skills` 增加 category/sort/search 筛选；管理员 `GET /skills/admin/all`；前端 Explore 下拉筛选、`AdminAppsView`、路由与导航
- **状态**：已完成代码实现（请在仓库内执行 `npm run build` 做前端编译自检）
- **产出**：`backend/app/routers/skills.py`，`schemas/common.py`（`SkillAdminRow` / `PaginatedAdminSkills`）；`frontend` `ExploreView.vue`、`AdminAppsView.vue`、`api/skills.ts`、`router`、`App.vue`
