## 任务：前端 UI 品质升级（产品级 Skill Store）

- **目标**：按需求文档升级 variables、App 布局与各页面模板/样式，不改 API/router/store 逻辑。
- **状态**：已完成（请在 `frontend/` 本地执行 `npm run build`；本会话环境未能执行 shell）
- **产出**：`frontend/src/styles/variables.css`、`global.css`、`App.vue`、各 `views/*.vue`。
- **备注**：Dashboard KPI 使用内联 SVG；审批页「本月已审/通过率」为占位「—」（无新统计 API）；Explore 增加 `heroToneClass` 展示用函数。
